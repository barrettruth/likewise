[package]
name = "likewise-fuzz"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
afl = "0.15.10"
arbitrary = "1.3.2"
likewise = { path = "../", features = ["arbitrary", "bytes", "unicode"] }
